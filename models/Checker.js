export class Checker {
    constructor(color, row, col, direction) {
        this.color = color;
        this.row = row;
        this.col = col;
        this.direction = direction;
        this.isKing = false;
    }
}
